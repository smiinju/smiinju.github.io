# smiinju.github.io

Personal academic homepage for Minju Song.

## Publish

Create a public GitHub repository named exactly `smiinju.github.io`, upload all
three site files to the repository root, and commit them to the `main` branch.
In **Settings → Pages**, select **Deploy from a branch**, then choose
`main` and `/(root)`.
